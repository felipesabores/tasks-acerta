version: '3.8'

services:
  web:
    build: .
    container_name: tasks-acerta-web
    restart: always
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    # Uncomment if you need to pass env vars during build (requires args in Dockerfile)
    # or if you are using runtime env injection (advanced)
